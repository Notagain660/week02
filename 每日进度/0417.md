前端：按照controller的顺序把所有接口跑通了并且加了/auth的登录注册二合一主界面以及/posts作为主界面实现到管理员功能（管理员可见）、个人信息（可以跳转进好友和发消息相关）、消息中心（返回回复我的评论还有接收方为我的消息并且支持跳转到对应帖子/聊天室）、举报管理（管理员权限可以管理举报状态）、帖子跳转按钮（查看详情）和管理员权限下的置顶按钮


补充帖子修改时可以改状态组件

后端修改：
- 新增chat和comment的selectlist，controller接口+service方法（receiver/replyid）来与前端的跳转功能相对应
- post的browspost修改逻辑，加上可以按照前端返回的用户id筛选（用户id只在查看信息界面显示帖子时会返回，否则为null，即不筛选用户id）
